<div class="container">
  <div class="row title">
    <h3 class="mx-auto">ユーザー</h3>
  </div>
  <% @users.each do |user| %>
    <%= render 'users/user_card', user: user %>
  <% end %>
  <div class=" row">
    <div class="mx-auto mt-3">
      <%= paginate @users %>
    </div>
  </div>
</div>
